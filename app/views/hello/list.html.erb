<table border="1">
  <tr>
    <th>ISBN</th><th>title</th>
    <th>price</th><th>author</th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><%= book.isbn %></td>
    <td><%= book.title %></td>
    <td><%= book.price %></td>
    <td><%= book.author %></td>
  </tr>
<% end %>
</table>
